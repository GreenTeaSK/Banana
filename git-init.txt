init ----------> ใช้ครั้งเดียว ได้ .git ไว้เก็บของ 
>>ใช้ครั้งเดียวต่อ folder

git init แก้ครั้งเดียวต่อ folder เป็นการช่วยสร้าง landmark ไว้สำหรับเก็บของ

---สร้างโฟล์เดอร์ -- ใช้1ครั้ง

>>git init เป็นคำสั่งที่บอกให้ไฟล์ติดต่อกับgit จะได้ไฟล์ .git

