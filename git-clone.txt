clone
git clone 
ใช้copy repository

git clone ตามด้วย SSH clone URL  (ใช้สำหรับการ clone folder ลงมาทั้ง folder)
>>git clone คือการ colone file ที่อยู่บน server ลงมายังเครื่องของเรา

>> เป็นการดึงข้อมูลจาก github ลงมาไว้ใน local

>>git clone ตามด้วยURL ssh 